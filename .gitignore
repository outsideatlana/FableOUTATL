# Dependencies
/node_modules
/.pnp
.pnp.*

# Next.js
/.next/
/out/
next-env.d.ts

# Production build
/build
/dist

# Environment — NEVER commit real secrets. (.env.example IS committed.)
.env
.env*.local

# Vercel
.vercel

# Logs / debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.tsbuildinfo

# Editor / OS
.DS_Store
*.pem
.idea/

# Claude / session / tooling artifacts
.claude/
.playwright-mcp/
.remember/

# Archived legacy code (kept locally for reference, not part of the deploy)
/_archive/

# Leftover locked legacy dir (empty shell — delete once preview process closes)
/outsideatl/
